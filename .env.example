# Gemini API key for the receipt-scan route (https://aistudio.google.com/apikey).
GEMINI_API_KEY=

# Shared passcode required to call /api/scan-receipt. Pick anything and share
# it out-of-band with the people who use the scan feature.
SCAN_PASSCODE=
